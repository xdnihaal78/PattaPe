# PattaPe - AI Crop Doctor
# requirements.txt
#
# This file covers all Python dependencies for the backend server and ML pipeline.
# For the frontend (React/Vite), dependencies are managed via package.json.
#
# Installation:
#   pip install -r requirements.txt
#
# Note: PyTorch install varies by hardware. See https://pytorch.org/get-started/locally/
# for the correct command for your CUDA version or CPU-only setup.
# Example CPU-only: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# =====================================================================
# Backend - FastAPI server (backend/)
# =====================================================================

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pillow>=10.2.0
httpx>=0.27.0

# =====================================================================
# ML Pipeline (ml/)
# =====================================================================

# Deep learning framework - install separately per hardware target
# torch>=2.2.0
# torchvision>=0.17.0

# EfficientNet model backbone (used in ml/model.py)
timm>=1.0.0

# Grad-CAM visualization
grad-cam>=1.5.0

# OpenCV for image processing in gradcam.py
opencv-python>=4.9.0

# Scientific computing
numpy>=1.26.0
matplotlib>=3.8.0

# Gemini second-opinion layer (ml/gemini_explainer.py)
google-genai>=0.3.0

# Dataset utilities (ml/ingest.py and evaluation)
tqdm>=4.66.0

# =====================================================================
# Testing
# =====================================================================

pytest>=8.0.0
pytest-asyncio>=0.23.0
requests>=2.31.0
